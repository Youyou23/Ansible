---
# tasks file for nodejs

- name: Upgrade all packages
  yum:
    name: '*'
    state: latest
- name: Installing nodejs & npm 
  yum:
    name: "{{item}}"
    enablerepo: epel
    state: installed 
  with_items:
    - nodejs
    - npm